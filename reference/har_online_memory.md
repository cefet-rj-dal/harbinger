# Online memory policies

Memory policies define how much recent context an online Harbinger
session keeps in memory before each detection cycle.

The online layer treats memory as an execution policy rather than as a
property of the detector itself.
