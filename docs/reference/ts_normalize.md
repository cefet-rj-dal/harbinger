# Time Series Normalize

Transform data to a common scale, facilitating comparisons and analysis.

## Usage

``` r
ts_normalize(remove_outliers = TRUE)
```

## Arguments

- remove_outliers:

  logical: if TRUE (default) outliers will be removed.

## Value

a `ts_normalize` object.
