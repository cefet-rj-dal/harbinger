# Time Series Sliding Window Min-Max

Time Series Sliding Window Min-Max

## Usage

``` r
ts_swminmax(remove_outliers = TRUE)
```

## Arguments

- remove_outliers:

  logical: if TRUE (default) outliers will be removed.

## Value

a `ts_swminmax` object.
