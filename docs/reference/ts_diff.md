# Time Series Diff

Time Series Diff

## Usage

``` r
ts_diff(remove_outliers = TRUE)
```

## Arguments

- remove_outliers:

  logical: if TRUE (default) outliers will be removed.

## Value

a `ts_diff` object.
