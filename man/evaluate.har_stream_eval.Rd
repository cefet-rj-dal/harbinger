% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har_stream_eval.R
\name{evaluate.har_stream_eval}
\alias{evaluate.har_stream_eval}
\title{Evaluate a streaming trace}
\usage{
\method{evaluate}{har_stream_eval}(obj, detection, reference = NULL, probability_threshold = NULL, ...)
}
\arguments{
\item{obj}{A \code{har_stream_eval} object.}

\item{detection}{Trace data frame returned by \code{collect_trace()}.}

\item{reference}{Optional logical or numeric event reference aligned with the
observation index.}

\item{probability_threshold}{Optional DP threshold used to derive an
additional filtered detection vector.}

\item{...}{Unused.}
}
\value{
A named list with per-observation trace metrics and aggregate
summaries. The returned list always includes:
\itemize{
\item \code{summary}: aggregate DP and DL statistics;
\item \code{by_observation}: the original trace table.
}

When \code{reference} is provided, it also includes:
\itemize{
\item \code{hard_metrics}: standard final binary evaluation using \code{har_eval()}.
}

When \code{probability_threshold} is provided, it also includes:
\itemize{
\item \code{threshold}: the threshold value;
\item \code{threshold_detection}: logical vector derived from DP filtering.
}

When both \code{reference} and \code{probability_threshold} are provided, it also
includes:
\itemize{
\item \code{threshold_hard_metrics}: hard evaluation after DP thresholding.
}
}
\description{
Computes trace-level summaries and, when a reference label vector is
provided, combines them with the standard Harbinger hard evaluation.
}
