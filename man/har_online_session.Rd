% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har_online_session.R
\name{har_online_session}
\alias{har_online_session}
\title{Harbinger online session}
\usage{
har_online_session(
  source,
  detector,
  executor = har_online_refit_full(),
  warmup_size = 30,
  batch_size = 30,
  memory = har_memory_full(),
  mode = c("auto", "pull", "push")
)
}
\arguments{
\item{source}{Streaming source object. May be \code{NULL} for push-only sessions.}

\item{detector}{Any existing Harbinger detector.}

\item{executor}{Execution strategy. Defaults to \code{har_online_refit_full()}.}

\item{warmup_size}{Number of initial observations consumed before regular
streaming detection starts.}

\item{batch_size}{Number of new observations required to trigger one online
detection cycle.}

\item{memory}{Memory policy. Defaults to \code{har_memory_full()}.}

\item{mode}{Session mode:
\itemize{
\item \code{"auto"}: behaves like pull mode when a source is available and like
push mode when \code{source = NULL};
\item \code{"pull"}: observations are requested through \code{next_observation(source)};
\item \code{"push"}: observations must be provided explicitly through \code{ingest()}.
}}
}
\value{
A \code{har_online_session} object.
}
\description{
Creates and runs a streaming session that reuses existing Harbinger
detectors without changing their offline APIs. The session layer is
intentionally additive: it orchestrates ingestion, batching, memory
management, tracing, and evaluation support around the detector already
provided by the package.

The session supports:
\itemize{
\item pull sources through \code{next_observation()}
\item push ingestion through \code{ingest()}
\item full or bounded memory policies
\item explicit execution strategies
\item structured tracing for Detection Probability (DP) and Detection Lag (DL)
}
}
\examples{
source <- har_source_simulated(c(10, 11, 12, 20, 12, 11, 10))
session <- har_online_session(
  source = source,
  detector = hcp_page_hinkley(min_instances = 3, threshold = 1),
  warmup_size = 3,
  batch_size = 2
)
session <- daltoolbox::fit(session)
session <- run_online(session)
head(collect_detection(session))
}
